# 待併入團隊手冊 — 2026-08-23（雲端 session 產出）

> **這份是給 Benson 帶回電腦端用的。** 雲端 session 看不到 Obsidian，寫不進手冊正本，
> 所以把跨專案通用的教訓寫在這裡，由你 pull 回電腦後併進 `qc/team-lab handbook` 正本。
>
> **併完就可以刪掉這個檔。** 專案專屬的知識不在這裡，在 `xd1104/movie-library` 的 `CLAUDE.md`（共 26 條）。
>
> 來源：做「好雷嗎」電影評價 App（新專案 `xd1104/movie-library`）的整個過程，
> 走完 lab-ux → lab-dev → lab-qa 五輪驗收（退件三次）。

---

## A. 一整組同家族的教訓：測量工具壞掉時，預設行為必須是「報錯」不是「報平安」

這次驗收挖出六條，全都是同一個病：**安全網自己壞了，卻回報一切安好**。
建議在手冊裡合成一節，因為它們是同一個判準的六個面向。

1. **突變測試要放一顆「無害改動」的對照組。**
   QA 的對照組兩度翻紅，兩次都證明它自己的測試在基準線就是紅的——沒有對照組的話，
   那份「QA 有抓到」的假訊號會被寫進交付報告。

2. **探針本身要自我校準。**
   QA 的 `location.reload` 偵測器根本是死的（jsdom 不允許 `defineProperty` 覆寫，
   失敗被自己的空 catch 吞掉），四條「沒有 reload」全是假通過，差一點就簽收。
   做法：**每一輪先故意觸發一次要偵測的行為，確認探針抓得到；抓不到就宣告整份結果不可信。**
   > 對照組管「程式沒壞時測試會不會亂紅」，自我校準管「程式壞了時測試會不會不紅」。兩個都要有。

3. **改完程式要確認突變還套得上。**
   突變的目標字串會隨重構失效，而失效的表現是**靜默跳過、不是紅燈**。
   這次 86 條裡有 9 條失效，另一輪又有 3 條因為版本號 +1 而失效。
   做法：突變工具要把「找不到目標字串」當成**失敗**；並提供 `--dry` 乾跑模式
   （只檢查套不套得上、不跑測試），重構完先跑那個，幾秒就知道。
   衍生：**突變表不可以寫死版本號**，要從 config 讀出來。

4. **安全網自己也要有離開碼。**
   突變工具算出「基準線本來就紅」，卻只印訊息不影響 exit code —— 結果基準線紅的時候，
   每一條突變都顯示「紅 ✓」，工具回報「每一件該守的事都有測試在守」，EXIT=0。
   **凡是會印出「⚠️／❌」的檢查，都要問一句「它有沒有接到離開碼」**；
   只印不擋的警告，在 CI 與趕時間的人眼裡等於不存在。

5. **豁免名單不只要斷言長度，「豁免項目變紅了」也要擋。**
   只做長度斷言的話，已經有人守的項目會一直以「預期全綠、不用管」的名義躺在名單裡，名單慢慢失真。
   實證：加了「豁免變紅也擋」當場就抓到一條過期的豁免。

6. **⭐「只允許 X」的斷言，如果測資裡沒有任何一筆「不是 X」，那條測試是空轉的。**
   實例：`ok(as.every(a => /href="https:\/\/www\.ptt\.cc\//.test(a)), "只連到 ptt.cc")`
   —— 看起來在防任意外連，但 fixture 裡的 url 本來就全是 ptt.cc，
   **程式裡沒有任何一行可以被拿掉來讓它變紅**（實際上根本沒實作白名單，是真的資安洞）。
   判準：**寫完一條「只允許 X」的斷言，就問「測資裡有沒有一筆不是 X」**；沒有的話它驗的是 fixture 不是程式。
   > 每加一條安全性斷言，順手加一條毒測資或對應突變。突變測試會抓到這種空轉。

7. **驗還原用的雜湊不要把 VCS 目錄算進去。**
   突變工具把 `.git/` 也算進 SHA-256，只要背景有任何東西跑過 `git status`，
   跑到一半就會誤判「還原失敗」exit 2。

---

## B. 測試不能只存在暫存區

dev 一開始把測試寫在 scratchpad，程式推上 GitHub 之後**防線等於不存在**——
下一個接手的人打開 repo 只看到一堆沒有測試的檔案。

- 測試要進 repo。加一個**只有 devDependency 的 `package.json`（例如 jsdom）不違反「零依賴」**
  —— 那條規則講的是**執行期**零依賴，上線的 App 仍然是純靜態、瀏覽器不下載任何函式庫。
  這點要寫進專案 CLAUDE.md，免得下一個人把 `package.json` 當違規刪掉。
- 對應地：**QA 自己的測試留在 scratchpad 是對的**（那是驗收方的工具），
  但 QA 抓到、而 repo 裡沒人守的項目，要明確要求 dev 補進 repo 的測試。

---

## C. 沒有真金鑰、沒有瀏覽器時怎麼驗

這台雲端機器**裝不了瀏覽器**（Playwright 下載 chromium 被 proxy 擋），且**連不到外部 API**。

- **用 jsdom 把真實 `index.html` ＋ 真實 JS 原封不動跑起來，只換掉 API**：
  驗得到 DOM 渲染、錯誤分類、狀態機，比讀程式碼推論可靠得多。
- **Service Worker 用 Node `vm` ＋ 假 `caches`／`Request` 真的跑一遍 install／activate／fetch**：
  特別適合驗「相對路徑有沒有解析到 GitHub Pages 子路徑」這種部署後才會炸的東西。
- **金鑰全程用假的**：要驗的是「沒有 key／key 錯／key 對」三條路徑的**行為**，
  不是真實回傳內容。工單直接寫死「不要向任何人索取真金鑰」。
- jsdom 沒實作的 API，測「它壞掉會怎樣」時要**先自己造出那個壞狀態**，否則測了個寂寞
  （實例：`'serviceWorker' in navigator` 在 jsdom 預設是 false，dev 的測試根本沒走到那條路徑）。
- jsdom 的 `localStorage` 是 Proxy，`localStorage.setItem = fn` 覆寫不了；
  模擬無痕要用 `Object.defineProperty(window,'localStorage',{get(){throw}})`。

---

## D. ⭐ 雲端 session 的網路限制，以及怎麼繞

**本機出口擋掉大量網域**（實測擋掉：`xd1104.github.io`、`www.ptt.cc`、`api.themoviedb.org`）。
意思是雲端 session **驗不了自己部署出去的網站，也連不到要爬的目標**。

**解法：把 GitHub Actions 當成「真實環境」，用它的執行紀錄當回饋迴路。**

- 程式先照假設寫好、用自製 fixture 驗邏輯 → push → 手動觸發 workflow → 讀 Actions log。
- 這次第一次跑就成功（PTT 爬蟲），而且 log 裡的診斷數字直接指出下一個要修的問題。
- 前提是**工單要求爬蟲自己回報健康度**：抓到 0 篇、或 0 篇帶標籤，就 exit 1 並印診斷
  （URL、HTTP 狀態、HTML 前 500 字）。**絕不可以安靜產出空檔案**——
  那會變成「App 顯示沒有資料」，沒人知道是爬蟲死了。
- PM 讀得到 Actions log（GitHub MCP 工具），所以這個迴路是通的。

**另一個實例**：`icon` 換了但老闆手機看到舊的，我**無法自己驗證線上檔案**（連不到 Pages），
只能請他用無痕分頁測。**這種時候要老實說「我驗不了，要你幫我測」**，不要猜。

---

## E. 已存起來的偏好值要改預設，必須換 key 做遷移

老闆要求改排序預設值。**只改程式裡的預設值是無效的**——
他手機的 localStorage 已經存著舊值，會一直用舊的，改了等於沒改。

做法：**換一個新的 storage key**（`hlm_sort` → `hlm_sort2`），讀不到就給新預設，並順手刪掉舊 key。
驗收要專門測「舊裝置帶著舊值開 App，會拿到新預設」。

> 通則：**以後要改任何已存起來的預設值都要用這招。**

---

## F. PM 的判斷題

1. **老闆在知情下選了你不推薦的方案 → 照做，但要把決策來源寫進 CLAUDE.md。**
   實例：排序改成「完全依熱門」，我明講過缺點（「跟直接打開 Netflix 首頁沒兩樣」），他仍選 A。
   關鍵是**原本的 CLAUDE.md 那條寫了相反的理由**——不更新的話，下一個人會照那條理由把它當 bug 改回去。
   寫法：把老闆原話、PM 提過的疑慮、以及「這不是疏忽，不要改回去，想動先問他」一起寫進去。

2. **子代理說「這條突變殺不掉是因為我做了兩層防禦」時不要直接採信。**
   跟「那條沒過是我測試斷言寫錯」是同一種形狀。QA 逐層拆開驗，結果是
   **不是兩層對等，其中一層在目前程式裡永遠觸發不到**，dev 的描述不準確。

3. **「醜」要先驗有沒有壞，再談設計。**
   這次 icon 被嫌兩次，查過確實是純美感問題（圖有正常畫出來、顏色也對），才交給 lab-ux 做多版讓他挑。
   反例在手冊已有（keyring 的透明按鈕）。

4. **純視覺的取捨就做幾版讓他挑，別用文字描述。**
   icon 做了 4 個明顯不同方向（場記板／「雷」字／整格底片／播放鍵＋閃電），他一句話就選完。
   工單要求**設計師自己先縮到 64px 看過、糊掉的自己淘汰**，不要交給老闆替它判斷。

5. **PM 不要什麼都問老闆。**
   這次 UX／QA 一共丟上來 20 幾個「要拍板」的點，我自己裁掉八成，只把
   「改變產品哲學」與「他會天天看到」的升級給他。

---

## G. 雜項技術雷

- **HTTP header 只吃 latin-1**。User-Agent 寫中文，`fetch` 會直接丟 `ByteString` 例外
  —— 第一次上線每一個請求都會炸。（dev 在收尾實跑時抓到，本機測試沒發現，因為測試用假 fetch。）
- **iOS 的主畫面圖示是「加到主畫面」當下就烙進去的**。換了 icon 之後，
  就算 SW 更新了，也要把捷徑刪掉重加才會變。**測試前先講一聲**，免得以為沒生效。
- **PWA 檔名沒變、只有內容變**（例如重畫 icon），一定要把版本號 +1，
  否則已安裝的裝置會繼續從舊快取吃到舊檔。
- **同源的資料檔要明確在 SW 裡放行**。原本的同源分支會把任何 200 回應動態塞進殼快取，
  結果是每天更新的資料檔被凍到下次改版——**而且完全沒有症狀**，是最像「沒壞」的壞。

---

## H. 這次適合抄的工單寫法

- **PM 先定死資料格式契約**，讓設計線與程式線並行（這次 lab-ux 做畫面、lab-dev 做爬蟲，同一份 JSON 契約）。
- **工單裡要寫「不要做的」**。這次每一份都寫，子代理沒有一次越界動到別的專案或順手改別的東西。
- **工單要求「過程中發現但不在清單上的問題，寫進報告，不要留在對話裡」** —— 實際收到好幾條有價值的自主發現。
- **要求子代理誠實標註「哪些是實測的、哪些只驗到邏輯」**，這次 dev 與 QA 都主動講了自己沒驗到的部分，
  包括各自承認一次「我上一輪那條測試是假通過」。

---

## I. ⭐ Benson 明確要求的溝通方式（2026-08-24 他親口說，這條優先）

> 「以後對我的一些說明可以盡量簡單易懂一點，我不想花太多時間去理解你的一些技術細節，
> 跟我說重點就好，這個你要記著」

背景：這場對話裡他說過兩次「聽不懂」「什麼？」，都是因為 PM 把驗收細節、
分支策略、模組版本落差這類東西整段講給他聽。

**規則：**
- **預設只給結論與他要做的事。** 技術細節一律等他問再展開。
- 要他做決定時，用「選項＋一句話後果」，不要用技術語言描述選項。
- 有風險要提醒時，**一兩句講完就好**，然後直接說我打算怎麼做——不要把整串推理攤給他。
- 進度回報：一句話狀態 ＋ 一句話下一步。不要貼測試數字、突變條數、檔案清單。
- 「他不看技術細節」在舊手冊 A 段就有，但**實測不夠強**——他被煩到主動要求了一次。
  這條要提到 A 段最前面。
