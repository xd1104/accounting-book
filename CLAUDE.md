# 記帳本（accounting-book）— 專案備忘

> 這份是接手的人／AI 的第一份說明書，也是**手機雲端 session 與 Benson 電腦之間唯一的橋**。
> 開工前讀完；收工後把新的決策寫回來。
> ⚠️ 這份骨架是在電腦端建立的，**內容大多還沒填**（只寫了從 README 與程式結構看得出來的部分）。
> 下次有人動這個專案時，請把真正的設計決策補進來——特別是「已定案、別誤改」那段。

## 這是什麼

給 Benson 自己用的記帳網頁 App。核心主張是**先規劃、再花錢**：每月先把薪水分配到各帳戶，
指定其中一個當零用錢來源，App 算出「今天還能花多少」。首頁的資訊架構刻意比照熱量 App。

完整功能清單見 `README.md`（寫得很完整，別重複寫在這裡）。

## 技術棧（⚠️ 跟 Benson 其他 App 不一樣）

**React 19 + TypeScript + Vite + Tailwind 4**，資料存 IndexedDB，PWA，GitHub Actions 部署（`.github/workflows/deploy.yml`）。

這點要特別注意：他其他的 web app（旅途手帳、食譜本、熱量）都是**零執行期依賴的原生 JS ＋ 本機 Node server**，
**這個專案不是**。不要把那套架構的習慣直接套過來，也不要「順手」把它改成那套。

```
src/components   src/lib   src/pages     （共 45 個檔）
scripts/generate-icons.mjs
```

## 怎麼跑

```
npm install
npm run dev        # Vite 開發伺服器
npm run build      # tsc -b && vite build
npm run typecheck  # 型別檢查，改完該跑
```

## 資料與同步

- 本機資料在 **IndexedDB**（照片會壓縮後存進去）
- 雲端同步是**選用**的，走 GitHub API＋token，兩種模式：資料放同一個公開 repo 的 `data/`，或另開一個 Private repo
- `data/**` 刻意**不觸發部署**——記帳不該一直重建網頁
- 衝突只在「兩邊改到同一個月份」時才停下來問，不會默默覆蓋

## 已定案、別誤改

看起來像 bug、其實是刻意的。**改之前先看理由**。

### 資料與同步
- **`data/**` 不觸發部署**（`deploy.yml` 的 `paths-ignore`）。記帳不該一直重建網頁。
- **同步衝突只在「兩邊改到同一個月份」才擋**。分月存檔就是為了讓不同月份自動合併。
- **`meta.md` 沒有全域 `updatedAt`**。同步是比對檔案內容；會隨任何編輯變動的時間戳
  會讓這個檔在兩台裝置上同時「看起來改過」，每次跨裝置編輯都變成衝突。
- **`migrate()` 對 `walletId` 只補「欄位不存在」的情況**（`'walletId' in a`），
  不補 `null`。`null` 是使用者在項目編輯器按「不指定」的結果，補掉等於偷改他的選擇。
- **`migrate()` 很寬鬆是故意的**（舊檔、手改過的檔都要能開），但**匯入必須先擋**
  （`looksLikeBackup`）。不擋的話任何 JSON 都會被它補成一本空帳然後覆蓋掉現有資料。
- **照片刪除是安全的**：只在帳目同步成功後、對「合併後的完整帳目」判斷；
  列表拿不到 sha 就跳過不刪。
- **同步請求不要加 `Cache-Control` 這類非 CORS 安全清單的標頭**。它會讓 preflight
  多一項要協商，是「連線成功但同步 Failed to fetch」的可疑來源。改用網址參數擋快取。

### 預算計算
- **收入也要算進零用錢**（`touchesAllowance` 兩種 type 都算）。退款、還錢是可以再花的。
  想排除某筆就把「算在」留空。
- **`bucketOf` 用日曆月**（`date.slice(0,7)`），刻意不跟 `monthStartDay` 綁；
  不然改發薪日會重寫所有檔案。
- **只結轉現金，不結轉戶頭**（`cashCarryByWallet`）。戶頭那筆錢從沒離開戶頭，
  每月再算一次等於重複發放。透支也照樣結轉，只結轉正的等於每月白送錢。
- **結轉用「從最早的期間往前疊加」而不是遞迴**：每一期的結餘本來就含它收到的結轉。
  沒有零用錢來源的期間不計入，否則第一份計畫之前的月份會憑空生出餘額。
- **首頁的環用 `progress` 判色，不能只用 `spentToday / todayBudget`**。
  額度已經是負的時候今天花 0 會算出 0%，就會出現「綠色的超支」。

### 版面
- **版本號由 build 時的 commit 產生**，同時寫進 JS 與 `sw.js` 的 cache 名稱。
  手動維護版本號忘了改 → 瀏覽器看不出 service worker 有變 → 手機永遠停在舊版，無症狀。
- **分配項目的金額欄是無邊框的**，只有 focus 才有底色。12 列都畫成輸入框會很吵。
- **刪除按鈕收在「編輯」模式裡**。預設列出 12 個垃圾桶只是噪音。
- **「零用錢放在哪」只列真的有錢的存放處**，其餘收成 chip。一排 0 不是資訊。

## 測試

沒有測試框架，是一組 Playwright 腳本（在 session 的 scratchpad，不在 repo 裡）。
跑法：`npm run build && npm run preview -- --port 4173`，再對 `localhost:4173` 跑。

**寫測試的雷（踩過兩次）：**
- **不要寫死月份或日期**。跨月當天整組會壞。用「相對今天」算出月份。
- **不要假設「今天不是當期第一天」**。1 號跑的時候沒有「今天以前」可用。
- **多裝置測試每個 context 都要給同一個 `timezoneId`**。沒給的跑在 UTC，
  跨午夜時兩台會對不同的日期記帳，看起來像同步壞掉。
- **不要用 `input[type=number]` 的全域索引定位**。版面一改就錯位，而且會安靜地
  填到別的欄位、測試照樣「通過」到很後面才炸。用卡片範圍 + 名稱定位。

## 待辦 / 已知粗糙處

- 雲端同步只對假的 GitHub API 驗過；真實 API 只有 Benson 手動試過。
- 電腦端曾出現「連線成功但同步 Failed to fetch」，原因未確認（見上面 `Cache-Control` 那條）。
- `data/months/` 有幾個 `2026-09（0 筆）` 的空檔案，來源是切到未來月份時寫出空檔，未清。
- **iPhone 上底部分頁列下方會露出一條頁面底色**（明細／統計特別明顯）。在一般瀏覽器
  量過 `gapUnderNav: 0`，我們的版面是對的，**原因未確認**、也無法在這裡重現。
  已在 nav 內加一層畫到自身下緣之外的底色當作保險（`-bottom-32`），但**沒驗證過有沒有效**。
  下次有人碰：先問 Benson 是「主畫面 App」還是 Safari 分頁，兩者的底部行為不一樣。

---

## 給 AI 的規則

### 開工前
1. 讀這份 CLAUDE.md ＋ `README.md`。
2. **有份量的任務**（做新功能、改架構、跨多檔案）→ 建議 Benson 打 `/team-lab` 開團隊，走
   lab-ux（先做 demo 給他玩）→ lab-dev（實作）→ lab-qa（會真的退件）的完整鏈。
   **不要自己預設開團隊**——小事（改錯字、單點修正、回答問題）自己做完就好，開團隊很花額度。
   判斷「這件事該用團隊」時**主動提醒他一句**，由他決定要不要開。
3. 團隊的角色與經驗手冊在 `.claude/`（`agents/` 三個角色、`skills/team-lab/handbook.md` 是跨專案累積的經驗）。

### 如果這個 repo 裡沒有 `.claude/`（雲端 session 特別注意）
你是雲端 session 的話，**只看得到這個 repo**，看不到 Benson 電腦上的設定與 Obsidian 記憶。
若發現這個 repo 缺少 `.claude/`（沒有團隊角色、沒有手冊），**先問 Benson 一句**：

> 「這個專案還沒有團隊技能與經驗手冊，要我從 `xd1104/claude-plugins` 抓一份下來嗎？」

他同意再抓（`plugins/ai-team/` 底下的 `agents/` 與 `skills/`）。**不要自己默默下載**，也不要沒有就硬做。

### 收工後（強制）
- 這次的決策、踩到的雷、「別誤改」的界線 → **寫回這份 CLAUDE.md**。
  雲端 session 看不到 Obsidian，**這份檔案是唯一會被帶回 Benson 電腦的東西**——沒寫進來的經驗就是掉了。
- 跨專案通用的教訓 → 回報時明講「這條該進手冊」，讓 Benson 帶回電腦端更新正本。
- 沒有新東西就不硬寫。

### 溝通
- 繁體中文、**結論先給短的**；Benson 是老闆、不看技術細節，深問再展開。
- **不確定就說不確定**，判斷不了的事要講出來，不要自己下結論也不要沉默放行。
- 察覺他有「不知道自己不知道」的盲點時主動反問；不可逆動作（刪除／送出／對外／改設定）前先問。
- 改完並確認完成後主動 commit + push（他的長期授權）。
