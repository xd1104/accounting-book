# 記帳本（accounting-book）— 專案備忘

> 這份是接手的人／AI 的第一份說明書，也是**手機雲端 session 與 Benson 電腦之間唯一的橋**。
> 開工前讀完；收工後把新的決策寫回來。
> ⚠️ 這份骨架是在電腦端建立的，**內容大多還沒填**（只寫了從 README 與程式結構看得出來的部分）。
> 下次有人動這個專案時，請把真正的設計決策補進來——特別是「已定案、別誤改」那段。

## 這是什麼

給 Benson 自己用的記帳網頁 App。核心主張是**先規劃、再花錢**：每月先把薪水分配到各帳戶，
指定其中一個當零用錢來源，App 算出「今天還能花多少」。首頁的資訊架構刻意比照熱量 App。

完整功能清單見 `README.md`（寫得很完整，別重複寫在這裡）。

## 技術棧（⚠️ 跟 Benson 其他 App 不一樣）

**React 19 + TypeScript + Vite + Tailwind 4**，資料存 IndexedDB，PWA，GitHub Actions 部署（`.github/workflows/deploy.yml`）。

這點要特別注意：他其他的 web app（旅途手帳、食譜本、熱量）都是**零執行期依賴的原生 JS ＋ 本機 Node server**，
**這個專案不是**。不要把那套架構的習慣直接套過來，也不要「順手」把它改成那套。

```
src/components   src/lib   src/pages     （共 45 個檔）
scripts/generate-icons.mjs
```

## 怎麼跑

```
npm install
npm run dev        # Vite 開發伺服器
npm run build      # tsc -b && vite build
npm run typecheck  # 型別檢查，改完該跑
```

## 資料與同步

- 本機資料在 **IndexedDB**（照片會壓縮後存進去）
- 雲端同步是**選用**的，走 GitHub API＋token，兩種模式：資料放同一個公開 repo 的 `data/`，或另開一個 Private repo
- `data/**` 刻意**不觸發部署**——記帳不該一直重建網頁
- 衝突只在「兩邊改到同一個月份」時才停下來問，不會默默覆蓋

## 已定案、別誤改

看起來像 bug、其實是刻意的。**改之前先看理由**。

### 資料與同步
- **`data/**` 不觸發部署**（`deploy.yml` 的 `paths-ignore`）。記帳不該一直重建網頁。
- **同步衝突只在「兩邊改到同一個月份」才擋**。分月存檔就是為了讓不同月份自動合併。
- **`meta.md` 沒有全域 `updatedAt`**。同步是比對檔案內容；會隨任何編輯變動的時間戳
  會讓這個檔在兩台裝置上同時「看起來改過」，每次跨裝置編輯都變成衝突。
- **`migrate()` 對 `walletId` 只補「欄位不存在」的情況**（`'walletId' in a`），
  不補 `null`。`null` 是使用者在項目編輯器按「不指定」的結果，補掉等於偷改他的選擇。
- **`migrate()` 很寬鬆是故意的**（舊檔、手改過的檔都要能開），但**匯入必須先擋**
  （`looksLikeBackup`）。不擋的話任何 JSON 都會被它補成一本空帳然後覆蓋掉現有資料。
- **照片刪除是安全的**：只在帳目同步成功後、對「合併後的完整帳目」判斷；
  列表拿不到 sha 就跳過不刪。
- **同步請求不要加 `Cache-Control` 這類非 CORS 安全清單的標頭**。它會讓 preflight
  多一項要協商，是「連線成功但同步 Failed to fetch」的可疑來源。改用網址參數擋快取。

### 預算計算
- **收入也要算進零用錢**（`touchesAllowance` 兩種 type 都算）。退款、還錢是可以再花的。
  想排除某筆就把「算在」留空。
- **`bucketOf` 用日曆月**（`date.slice(0,7)`），刻意不跟 `monthStartDay` 綁；
  不然改發薪日會重寫所有檔案。
- **只結轉現金，不結轉戶頭**（`cashCarryByWallet`）。戶頭那筆錢從沒離開戶頭，
  每月再算一次等於重複發放。透支也照樣結轉，只結轉正的等於每月白送錢。
- **結轉用「從最早的期間往前疊加」而不是遞迴**：每一期的結餘本來就含它收到的結轉。
  沒有零用錢來源的期間不計入，否則第一份計畫之前的月份會憑空生出餘額。
- **首頁的環用 `progress` 判色，不能只用 `spentToday / todayBudget`**。
  額度已經是負的時候今天花 0 會算出 0%，就會出現「綠色的超支」。

### 樣式（2026-09-02 UX 稽核，每一條都有人踩過）
- ⭐ **`src/index.css` 的 `input, select, textarea { font-size: 16px }` 刻意寫在
  所有 `@layer` 之外。** 無層級宣告贏過所有 Tailwind utility，這正是要的效果：
  **iOS Safari 對 font-size < 16px 的欄位會自動放大**，放大後點不到底部按鈕。
  **不准「順手整理」把它搬進 `@layer base`** —— 那會讓 `text-sm` 贏回來、bug 全部復活。
  原本這條也含 `button`，導致 27 顆按鈕的 `text-*` 全部失效（「本月收入」宣告 30px
  實際渲染 16px、比旁邊的 `$` 還小）—— `button` 已移除，**不准加回來**。
  代價：28 個 input/select/textarea 上的 `text-sm`/`text-xs` 是死碼，改不動也別當真。
- **同一個元素不准同時寫 `pb-*` 和 `safe-b`。** Tailwind 4 把 `.safe-b` 排在 `.pb-4`
  之後，`padding-bottom` 會**靜默變成 0**（sheet footer 就這樣掉了 16px 內距）。
  要兩者兼具就用會相加的寫法，改完**實測 computed 值**確認。
- ⭐ **`--ok-ink` / `--warn-ink` 是文字專用，`--ok` / `--warn` 是填色專用。**
  同一個綠當文字畫在中性底上只有 3.3:1，但它同時是**首頁那個環**與超支警示的填色
  —— 改填色會動到環。所以文字另開一組，**不准合併回去**。
- **theme-color 統一 = `--surface`**（不是頁面底色）。三處要一致：`index.html` 兩個
  帶 `media` 的 meta（負責 JS 啟動前）、`manifest.webmanifest`、`store.tsx` 切主題時覆蓋。
  理由見上面那條 iPhone 底部縫隙。
- **淺色模式的 ink/muted/faint 三階灰實際上只剩兩階**，是刻意的取捨不是沒調好：
  在頁面底色 `#f4f4f7` 上要維持 4.5:1，灰階亮度上限就到那裡，faint 實測 4.51 已經
  貼著門檻。「零對比失敗」與「三階灰」在淺色模式下不能兼得。層次改靠字級與粗細。
- **統計頁長條圖是 `touch-action: pan-y`，不是 `none`。** 用 `none` 會讓手指落在圖表上
  時**整頁不能垂直捲動**（實測 scrollTop 0→0），而且沒有任何錯誤、測試照樣全綠。
  水平拖曳靠 pointer 事件 + 方向判定（位移 >6px 且 |dy|>|dx| 就還原選取、交給瀏覽器捲）。

### 效能（2026-09-02 效能稽核，都有實測數字）
- **同步只抓 sha 有變的月份**（`sync.ts` 的 `readRemote(cfg, lazy)`）。目錄列表本來就
  給了每個檔的 sha，而 GitHub 的 blob sha 是內容定址的 —— sha 一樣就是內容一樣。
  三年份帳本一次穩態同步 39 個請求 → 3 個。
  ⚠️ **衝突分支一定要 `fillSkipped()` 補抓**：衝突畫面顯示「雲端 N 筆記錄 · 覆蓋這台裝置」，
  按下去會蓋掉本機還沒上傳的修改，**少報一筆都不行**。
- **`budget.ts` 的交易分桶快取依賴「store 每次 mutate 都產生新的 data 物件」。**
  哪天有人把 store 改成原地修改陣列，這裡會**安靜地**回答上一版的數字。
- **service worker 的 navigate 是 network-first + 2 秒逾時**。純 network-first 的問題不是
  離線（離線 fetch 立刻失敗、267ms 就開起來），是**半死不活的訊號**：實測伺服器延遲 8 秒時
  空白等 8.1 秒。逾時後仍會把網路那份寫回快取 —— **不准拿掉這個寫回**，不然殼永遠不更新。
- **route-level code splitting 量過了，是負面的，不要再提。** 原型實測：gzip 101→80KB、
  **首屏沒變快**、第一次點統計分頁反而 307→428ms。這個 App 的 bundle 57% 是 react-dom。

### 版面
- **版本號由 build 時的 commit 產生**，同時寫進 JS 與 `sw.js` 的 cache 名稱。
  手動維護版本號忘了改 → 瀏覽器看不出 service worker 有變 → 手機永遠停在舊版，無症狀。
- **分配項目的金額欄是無邊框的**，只有 focus 才有底色。12 列都畫成輸入框會很吵。
- **刪除按鈕收在「編輯」模式裡**。預設列出 12 個垃圾桶只是噪音。
- **「零用錢放在哪」只列真的有錢的存放處**，其餘收成 chip。一排 0 不是資訊。

### 視覺重設計（2026-09-03，lab-ux 出 demo → dev 實作，首頁 C 案 + 分配頁 P2）
- **`App.tsx` 的分頁標題列只對「自己畫標題」的頁面關掉**（目前是 `/` 和 `/records`），
  不是對所有 `isTab` 關掉。統計、設定還沒有自己的標題列，這兩頁還是靠 App header
  顯示分頁名——**別把 `ownsHeader` 展開成 `isTab`**，那樣統計/設定的標題會憑空消失，
  這輪的視覺規格根本沒提到這兩頁。
- **首頁「今日卡」的超支底色只換 `bg-tint-bad`，圓角/陰影/內距不變**——超支不是換一張卡，
  是同一張卡染色。`--tint-bad` 是新 token（`:root`/`.dark` 各一色 + `@theme` 的
  `--color-tint-bad`），跟 `--ok-ink`/`--warn-ink` 那條「文字色/填色分開」是同一種道理：
  這色只用來畫底、不是文字色。
- **`Ring.tsx` 超支時的視覺是「整圈低飽和 + 12 點鐘一小段實心」，不是「畫出超出的比例」**。
  6.6 倍超支時後者幾乎整圈實心紅，就是上一輪 Benson 嫌醜的東西。改動請保留這個形狀，
  兩個 `<circle>`（`opacity=0.3` 整圈 + `strokeDasharray={c*0.075} c`）都要留著。
- **分配頁 P2：「各存放處合計」卡整張刪掉了**，功能被「還要轉」/「轉好了」兩段依戶頭
  分組的清單取代，直接當頁面主體（不是唯讀報表卡）。這兩段只在有非現金分配時才出現；
  只放現金或完全沒分配時，「還要轉」「轉好了」兩個標題整段都不渲染。
  ⚠️ **打勾邏輯完全沒動**（`toggleDone` 對整筆 `Allocation` 生效，拆分項目在每個
  存放處分別出現一列、金額只算那一份、打勾影響所有列）——新版只是換了個分組方式呈現
  同一份資料，不是重寫預算邏輯。
- **分配頁「本月分配」/「零用錢設定」/「零用錢放在哪」收進三條摺疊列**（`Plan.tsx`
  的 `CollapsibleRow`，展開狀態用 `useState`，**沒有寫進 IndexedDB**——這是畫面狀態）。
  「點名稱開項目編輯器」這個入口也跟著移進「本月分配」摺疊區——**這是行為改變**，
  Benson 已經在 demo 上點過確認，dev 這輪也補了 Playwright 斷言驗證展開後點得到。
  改分配金額因此要多一次點擊（先展開「本月分配」）才碰得到 `input[type=number]`，
  這是刻意的取捨，不是漏做。
- **月份切換從「兩行置中大字＋日期範圍」壓成一行**（`月份 · 日期範圍` 同一行小字），
  規格原本畫的是「月份收成 chip、帶 ▾ 展開成月份選單」，這次**沒有做月份下拉選單**
  ——規格沒給互動細節、也沒有 demo 可以照抄，貿然做一個新元件風險比照抄現有 ‹ › 導覽
  高。如果 Benson 之後真的想要月份 chip + 選單，那是新工單，不要就地腦補。
- **記錄列（`TxnRow.tsx`）不再顯示分類名稱字串**（有備註時只剩 emoji 代表分類），
  archived 的分類/存放處資料還在，只是列上不重複寫一次名字——**別把這個當成
  「封存資料讀不到」的 bug 去修**，是刻意拿掉重複資訊（見規格第 6 節）。
- **Records 的搜尋輸入框改成收合，靠頂列「🔍 搜尋」chip 展開**——收合狀態存在
  元件 `useState`，不寫進任何持久化資料。
- **新增了 `formatHomeDate()`（`date.ts`）給首頁頂列用**，回傳 `{ md, weekday }` 兩段
  （字重不同要分開）。**沒有動 `formatFullDate`**——照規格保留，即使目前唯一的呼叫端
  （舊版 `Home.tsx`）已經被這輪換掉了，之後如果真的沒人用可以另外清，這輪不動它。
- ⭐ **摺疊區不准吃掉警示。** P2 把「零用錢設定」收進 `CollapsibleRow` 之後，
  「零用錢來源這個月還沒分配到錢、所以每日額度是 0」這則警示就藏在裡面了 ——
  藏起來的警示等於沒有警示，而這則正好是在解釋「為什麼首頁額度是 0」。
  現在 `CollapsibleRow` 有 `warn?: boolean`：`allowanceUnfunded` 時收合摘要直接顯示
  「⚠️ 來源還沒分配到錢」並套 `text-warn-ink`。**之後再往摺疊區裡搬東西前，先問一句
  「這裡面有沒有使用者不展開就看不到、但他需要知道的事」。**

## 測試

沒有測試框架，是一組 Playwright 腳本（在 session 的 scratchpad，不在 repo 裡）。
跑法：`npm run build && npm run preview -- --port 4173`，再對 `localhost:4173` 跑。

**寫測試的雷（踩過兩次）：**
- **不要寫死月份或日期**。跨月當天整組會壞。用「相對今天」算出月份。
- **不要假設「今天不是當期第一天」**。1 號跑的時候沒有「今天以前」可用。
- **多裝置測試每個 context 都要給同一個 `timezoneId`**。沒給的跑在 UTC，
  跨午夜時兩台會對不同的日期記帳，看起來像同步壞掉。
- **不要用 `input[type=number]` 的全域索引定位**。版面一改就錯位，而且會安靜地
  填到別的欄位、測試照樣「通過」到很後面才炸。用卡片範圍 + 名稱定位。
  ⚠️ **分配頁改成摺疊區之後，這條的風險反而變低但沒有消失**：只要測試每次只展開
  「當下要操作的那一區」，同一時間頁面上就只有那一區的 number input，全域索引仍然
  對得上；但如果哪次同時展開兩個以上摺疊區（例如同時測「本月分配」和「零用錢放在哪」），
  索引立刻錯位又是無聲的。**改摺疊區相關的測試前，先確認同一時間只開一個**，
  或改用 `按鈕.locator('xpath=..')` 抓到那個摺疊列自己的容器再往下找 input。
- **量這個 PWA 一定要開 `serviceWorkers: 'block'`**，否則 reload 會隨機噴
  `ERR_HTTP_RESPONSE_CODE_FAILURE`，而且量到的是快取不是 App。
- **測 `touch-action` 相關的行為必須用 CDP 派真觸控事件。** Playwright 的 `mouse` API
  不受 `touch-action` 影響 —— 統計頁那個「整頁不能捲」的 bug 用 mouse 測是**全綠的**。
- **CDP 的 `Network.emulateNetworkConditions` 節流不到 service worker 發出的請求。**
  第一次量到「延遲 10 秒也 161ms 開起來」差點把問題判成不存在；要在伺服器端真的加延遲。
- **不准寫 `ok(true, '...')` 這種恆真斷言。** 曾經有一條躲在改動最大的區塊裡，
  等於在那裡開了一個覆蓋率天窗 —— 統計頁滑不動的 bug 就是從那個洞溜上線的。
  ⭐ **而且恆真斷言會偽裝。** 2026-09-03 在 `income.mjs` 抓到一條寫成
  `!a.includes(x) && !b.includes(y) === false || true` —— 運算子優先順序算下來
  `|| true` 讓左邊算什麼都不影響結果，`grep "ok(true"` 抓不到它。
  **稽核時要一起 grep `|| true`**，光看 `ok(true` 不夠。
- ⭐ **不准留「永遠紅」的斷言。** 跟恆真斷言是同一個病的兩面：一個永遠綠所以驗不到東西，
  一個永遠紅所以沒人看。觸控目標那條原本每次都報 9 個失敗（BackupChip 的命中區是
  `::after` 撐的、`getBoundingClientRect()` 量不到；分配頁那 8 個是這輪之前就這樣），
  現在改成 `ACCEPTED_SMALL_TARGETS` 逐項列舉選擇器放行。
  **放行的是「這個選擇器」不是「這個數量」** —— 拿掉任一項就會自己紅回來（實測驗過）。
  新出現的小目標照樣擋得住。
- **不要留同一支腳本的複本。** `h-full.mjs` 是某次改動前 `full.mjs` 的原封不動複本
  （只差 port 號），結果版面一改兩邊各壞各的、還得手動 `diff`+`patch` 同步回去。
  要跑不同 port 就把 port 抽成參數，不要整支複製一份。
- 測試腳本在 session 的 scratchpad、**沒有版控**，被覆寫就救不回來，也做不出 diff。
  改測試前先留一份 `__tmp__` 備份。
- ⭐ **量效能基準線時，先 `curl` 驗證基準線伺服器服務的真的是舊版產物。**
  dev 和 QA **各踩了一次**，兩次都量出「修前修後一樣快」的假結論（一次是量測期間
  HEAD 被 commit 了，一次是 `cd` 沒生效 + vite 自動換 port）。
  最乾淨的隔離法：**同一份 app bundle、只抽換要比較的那個檔**。
- 分桶快取的死角實測過了：**原地 `data.txns.push()` 會安靜地少算**（舊實作 118,585、
  新實作 18,586，無錯誤訊息）；原地改金額、改 settings 都還是對的。
  `store.tsx` 的 `mutate()` 永遠 spread 新物件，所以現況安全 —— 那條警語不要刪。

## 待辦 / 已知粗糙處

- 雲端同步只對假的 GitHub API 驗過；真實 API 只有 Benson 手動試過。
  （2026-09-02 效能那批經 lab-qa 追認通過：51 條同步專屬測試、887 條逐月數值比對、
  隨機兩裝置 12 輪交錯編輯零遺失、衝突畫面筆數正確。但**仍然全是假伺服器**。）
- 電腦端曾出現「連線成功但同步 Failed to fetch」，原因未確認（見上面 `Cache-Control` 那條）。
- `data/months/` 有幾個 `2026-09（0 筆）` 的空檔案，來源是切到未來月份時寫出空檔，未清。
- ⚠️ **`sw.js` 的「資產」分支沒有時限**（只有 navigate 那條有 2 秒逾時）。
  後果：**第一次造訪之後馬上離線 = 永遠空白**。原因是第一次載入時 SW 還沒接管，
  那次的 JS/CSS 請求沒經過 fetch handler，所以沒進快取（實測：載入 1 次後快取裡
  只有 index.html 與圖示，載入 3 次後才有 hash 資產）。QA 實測斷線時 >20 秒空白，
  **修改前後一樣**，所以是既有問題、不是效能那批造成的。
  修法二選一：資產分支也加時限，或在 `install` 時把 hash 資產一起 precache。
- `sync/v1` 的 `#local` 存的是每個檔案全文，三年份約 540KB、localStorage 合計約
  992KB（5MB 配額的 19%）。懶抓現在依賴它存活。配額爆掉時 `saveSyncConfig` 會丟例外
  → 同步報錯 → 下次全抓（安全降級，不會掉資料）。十幾年份才會頂到，非急事。
- **視覺設計還沒做。** 2026-09-02 這輪是「稽核」——修的是量得出來的缺陷（對比、字級、
  觸控目標），Benson 的反應是「我看根本沒變」，**他是對的**。他嫌醜的東西還在：
  首頁超支時那個又粗又紅的環、備份提醒佔掉第一屏 1/4、三個數字卡沒有主次、
  記錄列太高、頂部「首頁」+ 日期兩行佔空間。**下一輪要走 lab-ux 出 demo → 他試玩
  確認方向 → dev 實作**，不要再從稽核直接跳到實作。
- 分配列的金額欄沒有千分位（群組小計寫 `$38,000`、緊鄰的輸入欄寫 `38000`），
  收入欄已經改了、分配列還沒，同一套邏輯應該一致套用。
- **iPhone 上底部分頁列下方那條 — 2026-09-04 修好了，機制跟原本的判斷不一樣。**
  幾何診斷是對的：iPhone 16 Pro 截圖（1206×2622 @3x）逐像素反推，**螢幕 874 CSS px、
  版面視口只到 812**，底下 62px 不在網頁的繪圖範圍內；分頁列 714→812 剛好 98
  （`h-16` 64 + safe-b 34），**`env(safe-area-inset-bottom)` 與 `viewport-fit=cover`
  都正常**（頁面底色一路鋪到螢幕 y=0，確認有蓋到瀏海）。
  ❌ **但「theme-color 會替那條上色」是錯的。** 量到那條是 `#f4f4f6`（= `--bg`），
  而當時 theme-color 三處都已經是 `#ffffff` 了。**決定那條顏色的是 body 的背景色**
  ——瀏覽器把 body 的背景 propagate 到 canvas、鋪滿整個視窗，theme-color 沒有參與。
  修法：`body.nav-canvas { background: var(--surface) }`，`App.tsx` 依 `isTab` 掛 class。
  頁面本身的底色由 App 根節點的 `bg-bg` 畫，所以換 body 的不影響畫面內容。
  ⚠️ **只在有分頁列的頁面換**：子頁面沒有分頁列，底下那條白的會變成沒來由的白邊。
  ⚠️ 這條在版面視口之外、**不是 DOM**，任何瀏覽器自動化都量不到它。
  能驗的只有「誰決定它的顏色」（`navcanvas.mjs` 驗 body 的 computed background
  ＋ 頁面本身沒有被一起換掉）。真機仍然沒驗過（這裡沒有 WebKit）。
  💡 教訓：**上一輪把「已診斷、不是我們的 bug」寫進待辦就停手了，其實只診斷到一半。**
  幾何算對不等於機制判對；顏色量一下就知道 theme-color 沒生效。
  ⚠️ **但「修好了」也只對一半，別照抄這個結論。** 換色後量他的新截圖：那條確實從
  `#f4f4f6` 變成 `#ffffff`（＝ canvas 換色成功，也再次證明決定它的是 body 背景），
  **但 Benson 的反應是「還是一樣沒改善」——他還是對的**：問題不是顏色是那 62px 的
  空白本身。以前是「版面漏一條灰的」，現在變成「分頁列下面一大塊白的」，
  分頁列連同空白共 160px 高、圖示只佔上面 64px。
  ❓ **還沒解開的矛盾**：他說他用的是主畫面 App（standalone），而 index.html 的
  `apple-mobile-web-app-capable` / `status-bar-style=black-translucent` /
  `viewport-fit=cover` 三個都在、`safe-t`/`safe-b` 也都量到有生效
  （頁面畫到螢幕 y=0、分頁列 714→812 剛好 64+34）。
  **standalone 的版面視口應該是整個 874，不該只有 812。**
  目前最合理的猜測是「主畫面圖示是在 index.html 還沒有這些 meta 之前加的」——
  iOS 會把 web-app 的視窗設定記在圖示上，之後改網頁不會更新。
  ⚠️ **驗證方式要小心：刪掉主畫面圖示可能一起刪掉 IndexedDB 裡的帳目與 token。**
  安全作法是「先另外加一個新圖示、不要刪舊的」比對。**沒驗證前不要動 `safe-b`**：
  猜錯方向而把安全區內距拿掉，真機上分頁圖示會被底部橫線壓到。

---

## 給 AI 的規則

### 開工前
1. 讀這份 CLAUDE.md ＋ `README.md`。
2. **有份量的任務**（做新功能、改架構、跨多檔案）→ 建議 Benson 打 `/team-lab` 開團隊，走
   lab-ux（先做 demo 給他玩）→ lab-dev（實作）→ lab-qa（會真的退件）的完整鏈。
   **不要自己預設開團隊**——小事（改錯字、單點修正、回答問題）自己做完就好，開團隊很花額度。
   判斷「這件事該用團隊」時**主動提醒他一句**，由他決定要不要開。
3. 團隊的角色與經驗手冊在 `.claude/`（`agents/` 三個角色、`skills/team-lab/handbook.md` 是跨專案累積的經驗）。

### 如果這個 repo 裡沒有 `.claude/`（雲端 session 特別注意）
你是雲端 session 的話，**只看得到這個 repo**，看不到 Benson 電腦上的設定與 Obsidian 記憶。
若發現這個 repo 缺少 `.claude/`（沒有團隊角色、沒有手冊），**先問 Benson 一句**：

> 「這個專案還沒有團隊技能與經驗手冊，要我從 `xd1104/claude-plugins` 抓一份下來嗎？」

他同意再抓（`plugins/ai-team/` 底下的 `agents/` 與 `skills/`）。**不要自己默默下載**，也不要沒有就硬做。

### 收工後（強制）
- 這次的決策、踩到的雷、「別誤改」的界線 → **寫回這份 CLAUDE.md**。
  雲端 session 看不到 Obsidian，**這份檔案是唯一會被帶回 Benson 電腦的東西**——沒寫進來的經驗就是掉了。
- 跨專案通用的教訓 → 回報時明講「這條該進手冊」，讓 Benson 帶回電腦端更新正本。
- 沒有新東西就不硬寫。

### 溝通
- 繁體中文、**結論先給短的**；Benson 是老闆、不看技術細節，深問再展開。
- **不確定就說不確定**，判斷不了的事要講出來，不要自己下結論也不要沉默放行。
- 察覺他有「不知道自己不知道」的盲點時主動反問；不可逆動作（刪除／送出／對外／改設定）前先問。
- 改完並確認完成後主動 commit + push（他的長期授權）。
